name: Deploy 🚀

on:
  push:
    branches:
      - main # Hoặc master tùy sếp nhé

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Thêm đoạn này để bảo chứng "nhân phẩm" cho con bot
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      # (Các bước build của sếp ở đây, ví dụ npm install && npm run build)

      - name: Deploy to GitHub Pages 🏗️
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # Dùng đúng cái này, không cần tạo cái khác đâu sếp!

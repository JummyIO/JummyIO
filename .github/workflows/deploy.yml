name: Deploy 🚀

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4 # 👈 Bước này để kéo code về nè sếp!

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 🕵️‍♂️ Bước "soi mói" để xem sếp đang ở đâu
      - name: List files 🔍
        run: ls -R

      - name: Install and Build 🏗️
        # Nếu package.json nằm trong thư mục con, sếp bỏ comment dòng dưới nhé:
        # working-directory: ./tên-thư-mục-con
        run: |
          npm install
          npm run build

      - name: Deploy to GitHub Pages 🚀
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          # Nhớ trỏ đúng đường dẫn đến thư mục dist đã build nhé
          build_dir: dist 
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
